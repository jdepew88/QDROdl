import {NextRequest,NextResponse}from"next/server";import path from"path";import Archiver from"archiver";import{buildViewModel}from"@/lib/viewModel";import{TEMPLATE_FILES}from"@/data/templates";import{renderDocxFromTemplate,docxBufferToPdfBuffer,saveDraftBuffer}from"@/lib/renderTemplates";export const runtime="nodejs";export async function POST(req:NextRequest){try{const{matterId,templateIds,format,delivery}=await req.json();if(!matterId||!templateIds?.length){return NextResponse.json({error:"Missing matterId or templates"},{status:400});}const vm=await buildViewModel(matterId);const rendered:{name:string;buf:Buffer}[]=[];for(const id of templateIds){const file=TEMPLATE_FILES[id as keyof typeof TEMPLATE_FILES];if(!file)continue;const base=path.parse(file).name;const docx=await renderDocxFromTemplate(file,vm);if(format==="pdf"){try{const pdf=await docxBufferToPdfBuffer(docx);rendered.push({name:`${base}.pdf`,buf:pdf});}catch{rendered.push({name:`${base}__PDF_FAILED__use_docx.docx`,buf:docx});}}else{rendered.push({name:`${base}.docx`,buf:docx});}}if(delivery==="save"){const links:string[]=[];for(const r of rendered){const stem=path.parse(r.name).name.replace(/__PDF_FAILED__use_docx$/,"");const ext=r.name.toLowerCase().endsWith(".pdf")?"pdf":"docx";const saved=await saveDraftBuffer({matterId,fileStem:stem,format:ext as any,buf:r.buf});links.push(saved.relative);}return NextResponse.json({ok:true,files:links});}const chunks:Buffer[]=[];const arch=Archiver("zip",{zlib:{level:9}});arch.on("data",(d)=>chunks.push(d as Buffer));const finalize=new Promise<void>((res,rej)=>{arch.on("warning",(e)=>console.warn(e));arch.on("error",rej);arch.on("end",()=>res());});for(const r of rendered)arch.append(r.buf,{name:r.name});arch.finalize();await finalize;const blob=Buffer.concat(chunks);return new NextResponse(blob,{status:200,headers:{"Content-Type":"application/zip","Content-Disposition":`attachment; filename="drafts_${matterId}.zip"`}});}catch(e:any){console.error(e);return NextResponse.json({error:e.message||"Render error"},{status:500});}}