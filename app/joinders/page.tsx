"use client";import{useState}from"react";export default function JoindersPage(){const[includeMailFiling,setIncludeMailFiling]=useState(false);const[loading,setLoading]=useState(false);const[err,setErr]=useState<string|null>(null);const startCheckout=async()=>{try{setLoading(true);setErr(null);const res=await fetch("/api/checkout/joinder",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({includeMailFiling})});const data=await res.json();if(!res.ok)throw new Error(data?.error||"Failed to start checkout");window.location.href=data.url;}catch(e:any){setErr(e.message);}finally{setLoading(false);}};return(<main className="mx-auto max-w-4xl px-4 py-10"><h1 className="text-3xl font-bold mb-6">Joinders</h1><section className="space-y-4 mb-10"><p className="text-gray-700">A <strong>Joinder</strong> makes the retirement plan a party to your case so the court’s order is enforceable. We only prepare joinders when required by the plan.</p><div className="rounded-2xl border p-4 bg-gray-50"><h2 className="font-semibold mb-2">Which plans typically require a joinder?</h2><ul className="list-disc list-inside space-y-1 text-gray-800"><li>CalPERS</li><li>CalSTRS</li><li>LACERA / other 1937 Act systems</li><li>Most DC plans (401(k), 403(b), 457, IAP) do not require joinder</li></ul></div><div className="grid md:grid-cols-2 gap-4"><div className="rounded-2xl border p-5"><h3 className="font-semibold mb-1">Our Policy</h3><p className="text-gray-700">We prepare joinders <strong>only</strong> when required by the plan administrator for a DRO/QDRO.</p></div><div className="rounded-2xl border p-5"><h3 className="font-semibold mb-1">Pricing</h3><p className="text-gray-700"><strong>$350 per joinder set</strong>. Optional mail filing adds court & mailing costs.</p></div></div></section><section className="rounded-2xl border p-6"><h2 className="text-xl font-semibold mb-4">Order a Joinder</h2><label className="flex items-center gap-3 mb-4"><input type="checkbox" className="h-4 w-4" checked={includeMailFiling} onChange={e=>setIncludeMailFiling(e.target.checked)} /><span>Include optional mail filing (fees added)</span></label>{err&&<p className="text-red-600 mb-3">{err}</p>}<button onClick={startCheckout} disabled={loading} className="px-5 py-3 rounded-xl bg-black text-white disabled:opacity-50">{loading?"Starting checkout...":"Purchase Joinder ($350)"}</button><p className="mt-3 text-sm text-gray-600">You’ll be redirected to Stripe to pay.</p></section></main>);}